{
    "name": "client",
    "version": "1.0.0",
    "dependencies": {
        "ejs": "*",
        "express": "*"
    },
    "engine": "node >= 0.10.0",
    "scripts": {
        "start": "node server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
